<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= note.title %></title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
</head>
<body>
    <div class="container">
        <h1><%= note.title %></h1>
        <p><%= note.body %></p>
        
        <div class="mt-4 d-flex">
            <!-- Update Button -->
            <a href="/dashboard/note/<%= noteID %>/edit" class="btn btn-primary me-2">Update</a>

            <!-- Convert Button -->
            <a href="/dashboard/note/<%= noteID %>/convert" class="btn btn-warning me-2">Convert</a>

            <!-- Delete Button -->
            <form action="/dashboard/note/<%= noteID %>?_method=DELETE" method="POST" style="display:inline;">
                <button type="submit" class="btn btn-danger me-2" onclick="return confirm('Are you sure you want to delete this note?');">Delete</button>
            </form>
            
            <a href="/dashboard" class="btn btn-secondary">Back</a>
        </div>
    </div>
    
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.3/dist/umd/popper.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
