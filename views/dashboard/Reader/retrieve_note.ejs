<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Retrieve Note</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-5">
        <h1 class="text-center">Retrieve Your Note</h1>
        <form action="/dashboard/retrieve" method="GET" class="mt-4">
            <div class="mb-3">
                <input type="text" name="key" required class="form-control" placeholder="Enter unique key" />
            </div>
            <button type="submit" class="btn btn-primary">Retrieve</button>
        </form>

        <% if (noteContent) { %>
            <div class="alert alert-success mt-4">
                <p>Your note can be accessed at the following URL:</p>
                <a href="<%= convertedUrl %>" class="alert-link"><%= convertedUrl %></a>
                <h2 class="mt-3">Note Content:</h2>
                <h3><%= noteContent.title %></h3>
                <p><%= noteContent.body %></p>
                <a href="/dashboard/note/view/<%= noteContent._id %>" class="btn btn-info mt-3">View Full Note</a>
            </div>
        <% } else if (error) { %>
            <div class="alert alert-danger mt-4">
                <p><%= error %></p>
            </div>
        <% } else { %>
            <div class="alert alert-info mt-4">
                <p>Enter the unique key to retrieve your note.</p>
            </div>
        <% } %>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
